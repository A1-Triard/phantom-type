[package]
edition = "2021"
name = "ensure_no_std"
version = "0.0.0"
publish = false

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]
educe = "0.5.11"
exit-no-std = "0.2.0"
phantom-type = { path = "..", default-features = false }
